Here are the grammar rules for the multi-function calculator. Most of them are
copied directly from tt(calc). Three rules, those which mention tt(VAR) or
tt(FNCT), are new:
        verbinclude(mfcalc/parser/grammar.rules)
