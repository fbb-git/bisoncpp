void \@Base::errorVerbose__()
{
    std::cout << "Parser State stack containing " << stackSize__() <<
                                                                "elements:\n"
                 "Each line shows a stack index followed "
                                    "by the value of that stack element\n";
    for (size_t idx = stackSize__(); idx--; )
       std::cout << std::setw(2) << idx << ": " <<
                    std::setw(3) << stackEl__<3>(idx) << '\n';
}

