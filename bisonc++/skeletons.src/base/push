void \@Base::push(size_t state)
{
$insert 4 debug "\nPUSH: " +
    checkStackSize();

    ++d_stackIdx;
    top() = StatePair{ d_state = state, std::move(d_val__) };

    d_vsp = &top();

$insert push
}

