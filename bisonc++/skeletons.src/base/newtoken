void \@Base::newToken__(int token)
{
    d_token = token;

    if (d_token <= 0)
        d_token = _EOF_;
$insert print
    ++d_acceptedTokens__;           // accept another token (see
}                                   // errorRecovery())

