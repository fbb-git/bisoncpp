void \@Base::newToken__(int lexToken)
{
    d_token = lexToken;

    if (d_token <= 0)
        d_token = _EOF_;

    ++d_acceptedTokens__;           // accept another token (see
                                    // errorRecovery())
}

