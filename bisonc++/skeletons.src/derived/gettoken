void \@::getToken__()
{ 
    if (token__() == _UNDETERMINED_)
    {
        newToken__(lex());
$insert print
    }

$insert 4 debug "getToken: token " << symbol__(token__()) << ", text: " << d_scanner.matched()
}
