To Install bisonc++ by hand instead of using the binary distribution or using
icmake's build script perform the following steps:

    1. Inspect the values of the variables in the file INSTALL.cf
       Modify these when necessary.

    2. Make sure the bobcat library has been installed.

    3. Run `./bootstrap' to compile and install bisonc++
